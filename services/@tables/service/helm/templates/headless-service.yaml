apiVersion: v1
kind: Service
metadata:
  name: {{ include "tables-service.fullname" . }}-headless
  labels:
    {{- include "tables-service.labels" . | nindent 4 }}
spec:
  clusterIP: None
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tables-service.selectorLabels" . | nindent 4 }}
