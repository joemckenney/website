---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: www-web
  namespace: default
spec:
  interval: 30m
  chart:
    spec:
      chart: ./apps/www/helm
      sourceRef:
        kind: GitRepository
        name: flux-system
        namespace: flux-system
      reconcileStrategy: Revision
      valuesFiles:
        - ./apps/www/helm/values.yaml
        - ./apps/www/helm/values-selfhosted.yaml
  values:
    image:
      tag: selfhosted-sha-ae07762e205ecaa9a3c74a236d656c76e2b8e010
