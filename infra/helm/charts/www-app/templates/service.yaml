apiVersion: v1
kind: Service
metadata:
  name: {{ include "www-app.fullname" . }}
  labels:
    {{- include "www-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "www-app.selectorLabels" . | nindent 4 }}
