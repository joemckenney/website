{
  "$schema": "https://turborepo.com/schema.json",
  "ui": "tui",
  "concurrency": "20",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "inputs": ["$TURBO_DEFAULT$", ".env*"],
      "outputs": ["dist/**"]
    },
    // Services that generate OpenAPI specs need spec/ in outputs for cache restoration
    "@gateway/service#build": {
      "dependsOn": ["^build"],
      "inputs": ["$TURBO_DEFAULT$", ".env*"],
      "outputs": ["dist/**", "spec/**"]
    },
    "@users/service#build": {
      "dependsOn": ["^build"],
      "inputs": ["$TURBO_DEFAULT$", ".env*"],
      "outputs": ["dist/**", "spec/**"]
    },
    // Spec packages output openapi.json to their root
    "@gateway/spec#build": {
      "dependsOn": ["^build"],
      "outputs": ["openapi.json"]
    },
    "@users/spec#build": {
      "dependsOn": ["^build"],
      "outputs": ["openapi.json"]
    },
    "@agent/spec#build": {
      "dependsOn": ["^build"],
      "outputs": ["openapi.json"]
    },
    // Agent service generates OpenAPI spec
    "@agent/service#build": {
      "dependsOn": ["^build"],
      "inputs": ["$TURBO_DEFAULT$", ".env*"],
      "outputs": ["dist/**", "spec/**"]
    },
    // Tables service generates OpenAPI spec
    "@tables/service#build": {
      "dependsOn": ["^build"],
      "inputs": ["$TURBO_DEFAULT$", ".env*"],
      "outputs": ["dist/**", "spec/**"]
    },
    "@tables/spec#build": {
      "dependsOn": ["^build"],
      "outputs": ["openapi.json"]
    },
    // Weather service generates OpenAPI spec
    "@weather/service#build": {
      "dependsOn": ["^build"],
      "inputs": ["$TURBO_DEFAULT$", ".env*"],
      "outputs": ["dist/**", "spec/**"]
    },
    "@weather/spec#build": {
      "dependsOn": ["^build"],
      "outputs": ["openapi.json"]
    },
    "check": {
      "dependsOn": ["^check"]
    },
    "lint": {
      "dependsOn": ["^lint"]
    },
    "format": {
      "dependsOn": ["^format"]
    },
    "db:up": {
      "cache": false
    },
    "db:down": {
      "cache": false
    },
    "dev": {
      "dependsOn": ["^migrate:deploy"],
      "cache": false,
      "persistent": true
    },
    "build:image:local": {
      "cache": false
    },
    "deploy:k8s:local": {
      "cache": false
    },
    "clean": {
      "cache": false
    },
    "migrate:deploy": {
      "dependsOn": ["db:up"],
      "cache": false
    }
  }
}
