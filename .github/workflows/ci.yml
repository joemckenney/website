name: CI

on:
  pull_request:
    branches:
      - main

jobs:
  build-images:
    name: Build Docker Images
    uses: ./.github/workflows/build-images.yml
    secrets: inherit
    with:
      environment: ci
      api_url: http://api.local.com
      tag_suffix: pr-${{ github.event.pull_request.number }}-${{ github.sha }}

  integration-test:
    name: Integration Tests (Minikube)
    runs-on: ubuntu-latest
    timeout-minutes: 20
    needs: build-images

    env:
      DOCKERHUB_USERNAME: crowprose
      IMAGE_TAG: pr-${{ github.event.pull_request.number }}-${{ github.sha }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Read Node version
        id: node-version
        run: echo "version=$(cat .nvmrc)" >> $GITHUB_OUTPUT

      - name: Read pnpm version
        id: pnpm-version
        run: echo "version=$(cat package.json | jq -r .packageManager | sed 's/pnpm@//' | sed 's/\"//g')" >> $GITHUB_OUTPUT

      - name: Setup pnpm
        uses: pnpm/action-setup@v2
        with:
          version: ${{ steps.pnpm-version.outputs.version }}

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ steps.node-version.outputs.version }}
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Start minikube
        uses: medyagh/setup-minikube@latest
        with:
          cpus: 2
          memory: 4096
          kubernetes-version: v1.28.0
          driver: docker

      - name: Enable minikube addons
        run: |
          minikube addons enable ingress
          minikube addons enable registry

      - name: Setup local registry
        run: |
          kubectl port-forward --namespace kube-system service/registry 5000:80 &
          sleep 5

      - name: Configure /etc/hosts
        run: |
          echo "$(minikube ip) www.local.com app.local.com api.local.com" | sudo tee -a /etc/hosts

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ env.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Pull and push images to local registry
        run: |
          # Pull from Docker Hub and push to minikube's local registry
          docker pull ${{ env.DOCKERHUB_USERNAME }}/www-app:${{ env.IMAGE_TAG }}
          docker pull ${{ env.DOCKERHUB_USERNAME }}/app-client:${{ env.IMAGE_TAG }}
          docker pull ${{ env.DOCKERHUB_USERNAME }}/app-server:${{ env.IMAGE_TAG }}

          # Tag for local registry
          docker tag ${{ env.DOCKERHUB_USERNAME }}/www-app:${{ env.IMAGE_TAG }} localhost:5000/www-app:latest
          docker tag ${{ env.DOCKERHUB_USERNAME }}/app-client:${{ env.IMAGE_TAG }} localhost:5000/app-client:latest
          docker tag ${{ env.DOCKERHUB_USERNAME }}/app-server:${{ env.IMAGE_TAG }} localhost:5000/app-server:latest

          # Push to local registry
          docker push localhost:5000/www-app:latest
          docker push localhost:5000/app-client:latest
          docker push localhost:5000/app-server:latest

      - name: Create Helm values for CI
        run: |
          # Create values-local.yaml for www-app
          cat > apps/@www/app/helm/values-local.yaml <<EOF
          replicaCount: 1
          image:
            repository: localhost:5000/www-app
            tag: latest
            pullPolicy: IfNotPresent
          ingress:
            enabled: true
            className: "nginx"
            hosts:
              - host: www.local.com
                paths:
                  - path: /
                    pathType: Prefix
            tls: []
          resources:
            limits:
              cpu: 200m
              memory: 128Mi
            requests:
              cpu: 100m
              memory: 64Mi
          EOF

          # Create values-local.yaml for app-client
          cat > apps/@app/app/helm/values-local.yaml <<EOF
          replicaCount: 1
          image:
            repository: localhost:5000/app-client
            tag: latest
            pullPolicy: IfNotPresent
          ingress:
            enabled: true
            className: "nginx"
            hosts:
              - host: app.local.com
                paths:
                  - path: /
                    pathType: Prefix
            tls: []
          resources:
            limits:
              cpu: 200m
              memory: 128Mi
            requests:
              cpu: 100m
              memory: 64Mi
          EOF

          # Create values-local.yaml for app-server
          cat > apps/@api/app/helm/values-local.yaml <<EOF
          replicaCount: 1
          image:
            repository: localhost:5000/app-server
            tag: latest
            pullPolicy: IfNotPresent
          ingress:
            enabled: true
            className: "nginx"
            hosts:
              - host: api.local.com
                paths:
                  - path: /
                    pathType: Prefix
            tls: []
          resources:
            limits:
              cpu: 200m
              memory: 128Mi
            requests:
              cpu: 100m
              memory: 64Mi
          env:
            NODE_ENV: production
            GOOGLE_CLIENT_ID: dummy-client-id
            GOOGLE_CLIENT_SECRET: dummy-client-secret
            GOOGLE_REDIRECT_URI: http://api.local.com/auth/google/callback
            JWT_ACCESS_SECRET: test-access-secret-key-for-ci-only
            JWT_REFRESH_SECRET: test-refresh-secret-key-for-ci-only
            FRONTEND_URL: http://app.local.com
          EOF

      - name: Deploy apps with Helm
        run: |
          # Deploy www-app
          helm install www-app apps/@www/app/helm -f apps/@www/app/helm/values-local.yaml --wait --timeout=3m

          # Deploy app-client
          helm install app-client apps/@app/app/helm -f apps/@app/app/helm/values-local.yaml --wait --timeout=3m

          # Deploy app-server
          helm install app-server apps/@api/app/helm -f apps/@api/app/helm/values-local.yaml --wait --timeout=3m

      - name: Wait for pods to be ready
        run: |
          kubectl wait --for=condition=ready pod -l app.kubernetes.io/name=www-app --timeout=120s
          kubectl wait --for=condition=ready pod -l app.kubernetes.io/name=app-client --timeout=120s
          kubectl wait --for=condition=ready pod -l app.kubernetes.io/name=app-server --timeout=120s

      - name: Run smoke tests
        run: |
          pnpm run test:smoke

      - name: Debug on failure
        if: failure()
        run: |
          echo "=== Pods ==="
          kubectl get pods -o wide
          echo "=== Services ==="
          kubectl get svc
          echo "=== Ingress ==="
          kubectl get ingress
          echo "=== Pod Logs (www-app) ==="
          kubectl logs -l app.kubernetes.io/name=www-app --tail=50
          echo "=== Pod Logs (app-client) ==="
          kubectl logs -l app.kubernetes.io/name=app-client --tail=50
          echo "=== Pod Logs (app-server) ==="
          kubectl logs -l app.kubernetes.io/name=app-server --tail=50

      - name: Cleanup
        if: always()
        run: |
          minikube delete

  ci-success:
    name: CI Success
    runs-on: ubuntu-latest
    needs: [build-images, integration-test]
    if: always()

    steps:
      - name: Check CI status
        run: |
          if [ "${{ needs.build-images.result }}" != "success" ] || \
             [ "${{ needs.integration-test.result }}" != "success" ]; then
            echo "❌ CI failed"
            exit 1
          fi
          echo "✅ All CI checks passed!"
