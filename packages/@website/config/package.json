{
  "name": "@website/config",
  "version": "0.0.0",
  "private": true,
  "files": [
    "*.json"
  ],
  "scripts": {
    "lint": "echo 'No lint needed for config package' && exit 0",
    "format": "echo 'No format needed for config package' && exit 0"
  },
  "exports": {
    "./base.json": "./base.json",
    "./react.json": "./react.json",
    "./node.json": "./node.json",
    "./biome": "./biome.json"
  }
}
