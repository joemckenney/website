{
  "name": "@website/squared",
  "version": "0.0.0",
  "type": "module",
  "private": true,
  "main": "./dist/squared.js",
  "types": "./dist/squared.d.ts",
  "scripts": {
    "build": "wasm-pack build --target bundler --out-dir dist && rm -f dist/.gitignore",
    "format": "biome format --write ."
  },
  "files": [
    "dist"
  ]
}
