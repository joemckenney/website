{
  "name": "@app/web",
  "version": "0.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "tsgo && vite build",
    "build:docker": "REPO_ROOT=$(pnpm exec topoftree) && eval $(minikube docker-env) && docker buildx build -t localhost:5000/app-web:latest --build-arg VITE_API_URL=http://api.local.com -f Dockerfile $REPO_ROOT",
    "build:docker:local": "REPO_ROOT=$(pnpm exec topoftree) && eval $(minikube docker-env) && docker buildx build -t localhost:5000/app-web:latest --build-arg VITE_API_URL=http://api.local.com -f Dockerfile $REPO_ROOT",
    "build:docker:prod": "REPO_ROOT=$(pnpm exec topoftree) && docker build -t ${DOCKERHUB_USERNAME}/app-web:${TAG:-latest} --build-arg VITE_API_URL=https://api.joemckenney.com -f Dockerfile $REPO_ROOT",
    "deploy:helm:local": "helm upgrade --install app-web ./helm -f ./helm/values-local.yaml --wait --timeout=5m",
    "deploy:helm:prod": "helm upgrade --install app-web ./helm -f ./helm/values-prod.yaml --wait --timeout=5m",
    "clean": "rm -rf dist",
    "clean:docker": "docker rmi localhost:5000/app-web:latest || true",
    "preview": "vite preview",
    "lint": "biome check --write .",
    "format": "biome format --write ."
  },
  "dependencies": {
    "@api/sdk": "workspace:*",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@website/topoftree": "workspace:*",
    "@website/tsconfig": "workspace:*",
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@typescript/native-preview": "7.0.0-dev.20251017.1",
    "@vanilla-extract/css": "^1.16.2",
    "@vanilla-extract/vite-plugin": "^4.0.18",
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^6.0.7"
  }
}
