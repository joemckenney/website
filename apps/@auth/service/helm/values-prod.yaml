# Production values for auth-service (auth.joemckenney.com)
# Secrets are injected via GitHub Actions using --set flags
replicaCount: 1

image:
  repository: crowprose/auth-service
  pullPolicy: IfNotPresent
  tag: "latest"

imagePullSecrets: []

service:
  type: ClusterIP
  port: 3001
  targetPort: 3001

ingress:
  enabled: true
  className: "nginx"
  annotations:
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
  hosts:
    - host: auth.joemckenney.com
      paths:
        - path: /
          pathType: Prefix
  tls:
    - secretName: auth-service-tls
      hosts:
        - auth.joemckenney.com

resources:
  limits:
    cpu: 100m
    memory: 256Mi
  requests:
    cpu: 50m
    memory: 128Mi

nodeSelector: {}
tolerations: []
affinity: {}

# Secrets - injected via GHA secrets (see cd.yml)
# These are placeholder values that get overridden by --set flags
secrets:
  googleClientId: ""
  googleClientSecret: ""
  googleRedirectUri: "https://auth.joemckenney.com/google/callback"
  jwtSecret: ""
  allowedEmail: "j.mckenney00@gmail.com"
  frontendUrl: "https://app.joemckenney.com"
